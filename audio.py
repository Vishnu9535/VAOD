import pyttsx3

def audio_output(text):
        # eng = pyttsx3.init()
        # rate = eng.getProperty('rate')
        # eng.setProperty('rate', rate - 15)
        # eng.say(text)
        # eng.runAndWait()
        ...